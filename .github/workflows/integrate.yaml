name: React App Github Deploy

on:
  push:
    branches:
      - "main"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install & build
        run: npm i && npm run build

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@4.1.1
        with:
<<<<<<< HEAD
          branch: gh-pages
          folder: dist
=======
          node-version: v18.16.0
          cache: 'npm'
      - run: npm ci
      - run: npm test
      - run: npm run build
      - name: Start server
        run: npm start


      
>>>>>>> a9ee9dc32bae7b6c3c1dc9ffdd293c5e1ef6b7b4
